cmake_minimum_required(VERSION 3.5 FATAL_ERROR)

project(generator)

message(STATUS "cmake is a cross-platform build system  \n for building projects in different structures according to different generators .")
message(STATUS "generator is actually used to build project for specified platform")


message(STATUS "In terms of generator type, the generators can be divided into three categories.")

message(STATUS "IDEs supported by cmake includes
visual studio series
NMake series
CodeBlocks series 
Eclipse serise
KDevelop3 series 
sublime text series
")
 
message(STATUS "relative generators supportted by cmake includes 
Makefiles
MinGW Makefiles
NMake Makefiles
Unix Makefiles
Ninja
")

message(STATUS "different IDEs and differernt generators collabroates to produce
more combinations.
")