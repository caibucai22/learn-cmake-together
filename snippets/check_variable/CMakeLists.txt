cmake_minimum_required(VERSION 3.20)

project(check_variable)

message("PROJECT_SOURCE_DIR =  ${PROJECT_SOURCE_DIR}") # 指向cmakelists.txt 所在目录
message("PROJECT_BINARY_DIR =  ${PROJECT_BINARY_DIR}") # 指向build 目录下